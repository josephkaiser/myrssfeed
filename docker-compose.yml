services:

  app:
    build: .
    restart: unless-stopped
    expose:
      - "8080"
    volumes:
      - ./feeds:/app/feeds   # persists rss.db across restarts

  nginx:
    image: nginx:stable-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx-docker.conf:/etc/nginx/conf.d/default.conf:ro
      - ./certs:/etc/ssl/myrssfeed:ro           # mount cert + key here
    depends_on:
      - app
